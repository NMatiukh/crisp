import React from 'react';
import style from './SizeCheckbox.module.scss';

const SizeCheckbox = () => {
    return (
        <div className={style.sizeCheckbox}>
            Test
        </div>
    );
};

export default SizeCheckbox;